__pycache__/
*.pyc
.DS_Store

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*-blx.bib

# Local PDF (generated by Actions)
Colonius.pdf

# Spreadsheets are NEVER committed — the master is private, and CI fetches a
# public-safe derived sheet at build time. Keep all .xlsx out of the repo.
*.xlsx
~$*.xlsx

# Generated site output — rebuilt by CI (gen_cv_lists.py + build.py).
# Source assets (docs/images/* — logos, banners, people photos, research) are tracked.
colonius.bib
/docs/*.html
/docs/style.css
/docs/cv.pdf
/docs/publications.bib
/docs/CNAME
